# 设备标签打印 / 扫码装箱 一体化工具

一个面向设备的 Windows 桌面工具，整合「设备标签打印」与「扫码装箱打印」两个模块，顶部标签页切换、互不干扰；单文件运行、无需外置 DLL，并支持自动更新。

运行环境：Windows 7 及以上（建议 Windows 10 以上），需要 .NET Framework 4.8。

## 版本

- **当前版本：v2.0.8**
- v2.0.8：新增**历史记录按日期筛选**——历史记录区右侧出现「日期」选择框，点开是自绘日历：**当天有记录的日期带浅灰色底**，鼠标停在任意日期上会提示“当天 N 条记录”，点某一天只显示那天的记录（不再一次性把全部历史堆在一起），并提供「今天」快捷键与左右翻月；打开软件默认停在“最近有记录的那一天”。同时做**多分辨率适配**：窗口按当前屏幕可用区域（已扣除任务栏）自动决定大小与最小尺寸，小屏（1366×768、1280×720、1024×600）打开不会再被任务栏挡住或裁掉底部；内嵌页面不再强制最小尺寸（这是之前历史记录底部按钮、装箱页底部被裁的原因）；拖到更小的显示器、改分辨率或改缩放比例时自动收回工作区。另修复「标签规格」宽/高输入框被卡片边框切边、以及窄窗口下装箱页「地区＋参与打印」被挤掉的问题。
- v2.0.7：修复历史记录相关两个问题——(1) 重开软件时读取**全部**按天历史文件（之前只读“今天”那一个文件，导致换个日期重开列表为空、看起来像历史被删）；(2) 保存历史改成**以磁盘现有文件为准**，只应用本机本次运行的新增/删除，**不再把外部修改或复制进来的历史文件覆盖清空**；“清空全部”改为直接删除历史文件。
- v2.0.6：修复扫码装箱多型号数量不累加的问题（第 2、第 3、第 4… 个型号再扫同型号设备时数量不增加）——自动填充的判重签名改为“型号+数量”，数量变化即刷新。
- v2.0.5：扫码装箱改为“按历史判断 SN/MAC”——每个扫到的码都去和「设备标签打印」历史对比：匹配到已知 MAC 当 MAC、已知 SN 当 SN，两者都不是则提示“无此设备标签”（不再用“第二条一律按 MAC”，避免连扫两个 SN 出错）；扫码区新增「强制录入（不对比历史）」勾选框，勾选后跳过对比直接录入。
- v2.0.4：修复扫码装箱“纯数字 MAC 扫不进”的问题（按扫码顺序判断，等待配对的第二条按 MAC 处理）；扫码区新增「重新录入」按钮（清除已扫一半、未配对的 SN/MAC）；扫码记录表最前面新增「序号」列（倒着记：最新最大，如 3、2、1），原“#”列改为「型号」；序号仅在列表显示，不参与打印/预览。
- v2.0.3：修复扫码装箱“MAC 扫不进”的问题（装箱只扫 SN/MAC，MAC 不再与设备标签打印历史对比，直接录入）；打印预览窗口在“保存位置”下方新增「关闭」按钮，避免窗口标题栏被顶出屏幕时无法关闭。
- v2.0.2：扫码装箱新增「地区」下拉（分组：成都 / 县级市 / 县，表头不可选，支持拼音/首字母查找），地区后加「参与打印」勾选（勾选才打到装箱标签上），并在装箱标签上新增「打印时间」；地区与勾选状态均可记忆。
- v2.0.1：统一“检查更新”入口。仅保留「设置 → 检查更新…」，指向合并版的最新版本；移除标签打印页与扫码装箱页各自的检查更新按钮，并彻底移除扫码装箱自带的旧更新逻辑，避免误覆盖。
- v2.0.0：重大改版。将「设备标签打印」与「扫码装箱打印」整合进一个程序，顶部双标签页、标题栏下菜单栏（文件/设置）；新增全局「测试模式」、NAS 全局备份（同步标签打印历史 + 装箱记录）、今日汇总报告发送（企业微信/飞书）、扫码装箱的型号识别/多型号录入/校验（只认设备标签打印里的设备），并做成单文件 exe。
- v1.3.7：修复 NAS 备份“有/无”开关在点击“有”时自动跳回“无”的问题。
- v1.3.6：NAS 备份改为按机器名建立独立子文件夹，多台电脑共用同一 NAS 目录时，各机器历史记录互不覆盖。
- v1.3.5：新增 NAS 数据备份。设置里可开启/关闭（有/无）NAS 同步并选择 NAS 目录，每 5 分钟对比并把本地新增的历史记录同步到 NAS（也支持“立即同步”）。
- v1.3.4：打印后保留最后一张预览（不再自动清空为假型号/假条码），关闭重开软件后也能恢复上次打印的内容到预览。
- v1.3.3：修复“关闭重开后今日已打印计数为 0”的问题（改为按记录录入日期统计，历史加载更健壮）；新增打印校验——要打印的内容（型号/类型/SN/MAC）缺失时提示并禁止打印。
- v1.3.2：修复左侧「扫码录入」面板底部提示文字被裁切的问题（改为换行显示 + 底部留白）。
- v1.3.1：修复“检查更新”报“无法连接更新服务器”。启用 TLS 1.2，并在配置的更新源无法访问时自动回退到 Gitee 默认更新源，国内网络也能正常检查更新。
- v1.3.0：界面改版。统一配色与卡片式布局（圆角分组、单一强调色、清晰按钮层级），表格与表单样式统一优化；保留全部原有功能与排版方式不变。

## 主要功能

- **两种录入方式**（左上角下拉框切换）
  - 二维码模式：扫码枪先扫设备二维码 → 自动识别出 型号/类型/SN → 再扫 MAC 条码。
  - 固定型号模式：手动填写 型号/类型（固定不变）→ 先扫 SN 条码 → 再扫 MAC 条码，适合型号固定的批量打印。
- **自动识别**：从二维码内容提取 型号、类型、SN；MAC 从第二条码读取，识别后每个字段都可手动修改。
- **标签设计**：包含 型号、类型、SN 条码＋文字、MAC 条码＋文字（Code128），支持上下堆叠布局、条码粗细自动适配。
- **预览与排版**：实时预览，鼠标可直接拖动文字/条码，选中后可改 字体大小、水平/垂直位置、条码高度；排版自动记住。
- **打印**：可勾选要打印的内容；可选择打印机，并能添加网络/局域网打印机；打印质量按打印机原生分辨率渲染（清晰、不缩放）。
- **自动保存**：每台设备自动写入 `数据\历史记录_日期.csv`（每天一个新文件），双击可重印，统计今日已打印数量；设置自动保存。
- **历史记录按日期查看**：历史记录区右侧「日期」框点开是日历，**有记录的日期带浅灰底**，悬停显示当天条数，选中某天就只看那天的记录；带「今天」快捷键与翻月。
- **多分辨率适配**：窗口大小按屏幕可用区域（扣除任务栏）自动决定，小屏笔记本打开不会被任务栏挡住；窗口变小或换屏时，左右两栏自动出现滚动条，历史记录区始终完整可见。
- **自动更新**：启动时自动检查新版本，也可点"检查更新"手动检查；发现新版提示下载、替换、重启，历史记录和设置不丢失。

## 使用方法

1. 双击 `设备标签打印.exe` 打开软件。
2. 首次使用：右侧选择打印机（如 TSC TTP-244 Pro），确认标签规格（默认 85×35mm）与打印精度（203 dpi）。
3. 选择录入方式，按提示扫描/填写，识别的设备会自动打印并保存（可在"扫描完成后自动打印并保存"复选框关闭）。
4. 历史记录在下方表格，双击可重印；想只看某一天，点历史记录区右侧的「日期」框选日期（有记录的日期是浅灰底）。
5. 检查更新在菜单栏「设置 → 检查更新…」。

## 构建与发布新版本

- 源码为单文件 `LabelPrinter.cs`，使用 .NET Framework 4.8 自带的 `csc.exe` 编译。
- 软件内置版本号：源码中的 `Updater.AppVersion`。
- 发布新版本步骤：
  1. 修改 `Updater.AppVersion` 为更大的版本号（如 1.4.0）。
  2. 重新编译得到新的 `设备标签打印.exe`。
  3. 上传到 Gitee 仓库的 `label-printer.exe`，并把 `latest.json` 里的 `version` 与 `download` 指向新版本。
  4. 如需走 GitHub 更新通道：在 GitHub 仓库的 Releases 页面新建发布，Tag 填 `v1.4.0`，把新的 exe 作为附件上传。
  5. 发布后，已安装的软件会自动检测到并升级。
- 更新接口：默认读取 Gitee 仓库的 `latest.json`；也可配置为 GitHub Releases（`releases/latest`）。仓库公开时无需令牌；若为私有仓库，需配置带只读权限的 Fine-grained Token。

## 数据

软件目录下会生成 `数据` 文件夹：`历史记录_日期.csv`（按天保存的设备记录，可用 Excel 打开）和 `设置.ini`（打印机、标签规格、排版等设置）。
